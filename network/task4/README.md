# Assignment 2, Task 4

key generated with OpenSSL with:

```sh
openssl req -x509 -newkey rsa:4096 -keyout volumes/key.pem -out volumes/cert.pem -sha256 -days 365
```

and the PEM pass phrase is `supersecret`

This could be changed for something more interesting later B-)
